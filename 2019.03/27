03.27 星期三
1、制作PPT,看文中安全性证明以及应用（是基于CRA可撤销的IBE方案），CRA辅助身份认证，在认证的过程中使用了基于身份加密，把密文中最后一项移出来，用作验证用户的身份，这个应用场景还是很新的。
2、其实本篇论文就是跟对基于身份的撤销问题进行了改进！在方案中解决了身份密钥的更新给PKG造成的负担，引入CRA帮助PKG生成可更新的密钥，PKG只需要发送给一个CRA一个主时间密钥（而不区分用户），用户的更新密钥在公开通道中传输，而且不影响安全性。
3、计划再结合着2001年的安全性证明看看！
